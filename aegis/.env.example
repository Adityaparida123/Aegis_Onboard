# Backend API base URL for the browser.
# Accepted names: VITE_API_URL or VITE_API_BASE_URL.
# Leave unset in development: Vite proxies /api to http://localhost:4000.
# Set in production (Vercel build env) to your deployed backend,
# e.g. VITE_API_URL=https://api.onrender.com/api
# VITE_API_URL=
